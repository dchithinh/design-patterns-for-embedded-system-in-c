cmake_minimum_required(VERSION 3.16)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(chapter6_04_channelmode C)
endif()

add_custom_target(chapter6_04_channelmode)
message(STATUS "chapter6/04-channelmode: placeholder target only. Header dependency order/type declarations need source cleanup.")
