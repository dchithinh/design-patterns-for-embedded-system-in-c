cmake_minimum_required(VERSION 3.16)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(chapter5_04_state_pattern C)
endif()

add_custom_target(chapter5_04_state_pattern)
message(STATUS "chapter5/04-StatePattern: placeholder target only. Example depends on missing runtime headers and incomplete sources.")
