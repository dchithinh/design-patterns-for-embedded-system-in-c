cmake_minimum_required(VERSION 3.16)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(chapter4_07_simulalock C)
endif()

add_custom_target(chapter4_07_simulalock)
message(STATUS "chapter4/07-simulaLock: placeholder target only. Source currently has compile issues and no runnable entry point.")
