cmake_minimum_required(VERSION 3.16)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(chapter5_05_and_state C)
endif()

add_custom_target(chapter5_05_and_state)
message(STATUS "chapter5/05-AndState: placeholder target only. Source currently has compile issues and missing runtime context.")
