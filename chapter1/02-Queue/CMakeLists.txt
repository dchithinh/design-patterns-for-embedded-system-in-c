cmake_minimum_required(VERSION 3.16)

if(CMAKE_SOURCE_DIR STREQUAL CMAKE_CURRENT_SOURCE_DIR)
  project(chapter1_02_queue C)
endif()

add_custom_target(chapter1_02_queue)
message(STATUS "chapter1/02-Queue: placeholder target only. Source currently does not compile cleanly enough for automated CMake build.")
